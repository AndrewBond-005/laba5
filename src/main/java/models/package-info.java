/**
 * пакет для хранения основных моделей в программе:
 * <p>
 * - {@code Coordinates.java}: работает с координатами
 *<br>
 * - {@code Country.java}: enum хранит значения страны
 *<br>
 * - {@code Element.java}: абстрактный класс для взможности получить id у главной модели
 *<br>
 * - {@code EyeColor.java}: enum хранит значение цвета глаз
 *<br>
 * - {@code HairColor.java}: enum хранит значение цвета волос
 *<br>
 * - {@code Person.java}: второстепенная(вложенная) модель
 *<br>
 * - {@code Position.java}: enum хранит значение позиции
 *<br>
 * - {@code Status.java}:enum хранит значене статуса
 *<br>
 * - {@code Validate.java}:интерфейс для проверкой корректности полей
 *<br>
 * - {@code Worker.java}: основная модель
 *<br>
 * </p>
 *
 * @since 1.0
 */
package models;